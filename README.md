# 🌐 WebBrowser.Coursework — Інструкція з налаштування

Цей проєкт є навчальним веб-браузером, розробленим на **WPF (.NET 8.0)** з використанням бази даних **PostgreSQL** для збереження історії та закладок.

Нижче наведена покрокова інструкція для розгортання бази даних та запуску проєкту.

## 🛠 1. Попередні вимоги (Prerequisites)

Перед запуском переконайтеся, що у вас встановлено:

1.  **Visual Studio 2022** (з навантаженням ".NET Desktop Development").
2.  **PostgreSQL** (версія 14 або новіша). [Завантажити тут](https://www.postgresql.org/download/).
3.  **pgAdmin 4** (зазвичай встановлюється разом з PostgreSQL) або будь-який інший SQL-клієнт (DBeaver, DataGrip).

-----

## 🗄 2. Налаштування Бази Даних

### Крок 2.1. Створення бази даних

1.  Відкрийте **pgAdmin**.
2.  Підключіться до вашого локального сервера.
3.  Створіть нову базу даних з назвою **`browser_db`**.

<!-- end list -->

```sql
CREATE DATABASE browser_db;
```

### Крок 2.2. Створення таблиць

Виконайте наступний SQL-скрипт у щойно створеній базі даних `browser_db` (через Query Tool), щоб створити необхідні таблиці:

```sql
-- Таблиця для збереження закладок
CREATE TABLE IF NOT EXISTS bookmarks (
    id SERIAL PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    url TEXT NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Таблиця для збереження історії відвідувань
CREATE TABLE IF NOT EXISTS history (
    id SERIAL PRIMARY KEY,
    title VARCHAR(255),
    url TEXT NOT NULL,
    visit_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

-----

## ⚙️ 3. Налаштування проєкту (Connection String)

Щоб програма могла підключитися до вашої бази даних, необхідно вказати правильний пароль у коді.

1.  Відкрийте проєкт у Visual Studio.
2.  Знайдіть файл **`MainWindow.xaml.cs`** (шлях: `WebBrowser.Coursework/MainWindow.xaml.cs`).
3.  Знайдіть рядок з константою `ConnString` (зазвичай на початку класу):

<!-- end list -->

```csharp
private const string ConnString = "Host=localhost;Username=postgres;Password=12345;Database=browser_db";
```

4.  **Змініть параметр `Password=12345`** на ваш реальний пароль, який ви задали при встановленні PostgreSQL.
      * Якщо у вас інше ім'я користувача (не `postgres`), змініть також параметр `Username`.
      * Якщо порт відрізняється від стандартного (5432), додайте параметр `Port=5432;`.

-----

## 📦 4. Залежності (NuGet Packages)

Проєкт використовує наступні пакети, які Visual Studio має відновити автоматично при першій збірці:

  * **Npgsql** — драйвер для PostgreSQL.
  * **Microsoft.Web.WebView2** — рушій браузера.

Якщо виникли помилки збірки, спробуйте виконати команду в Package Manager Console:

```powershell
Update-Package -Reinstall
```

або натисніть правою кнопкою на рішення -\> **Restore NuGet Packages**.

-----

## ▶️ 5. Запуск

1.  Натисніть **Build** -\> **Rebuild Solution**.
2.  Натисніть **Start** (або F5).
3.  Якщо все налаштовано вірно, відкриється вікно браузера, а дані про відвідування почнуть з'являтися у таблиці `history` вашої бази даних.
